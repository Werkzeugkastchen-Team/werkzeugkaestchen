{% extends 'base.html' %}

{% block content %}
    <h1>{% block title %} Digitale Werkzeuge für Ihren Arbeitsalltag {% endblock %}</h1>
    <h2>Tools Hauptseite</h2>

    <div class="row">
        {% for tool in toolsToRender %}
        <div class="col-md-4 mb-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{tool.name}}</h5>
                    <p class="card-text">Click to use this tool</p>
                    <a href="{{ url_for('tool_form', tool_name=tool.__class__.__name__) }}" class="btn btn-primary">Open Tool</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
{% endblock %}